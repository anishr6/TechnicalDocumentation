A complete document — use it as the reference for block order, spacing and voice when writing a new one.

```jsx
<DocsArticle />
```

Structure: DocHeader → h2 section → DocCard concepts → QuoteBanner of product copy → numbered
StepCards → AccentPanel detail → Callout → FAQ DocCards → FeedbackBar → DocFooter.
All copy is generic placeholder text; replace it, keep the structure.
