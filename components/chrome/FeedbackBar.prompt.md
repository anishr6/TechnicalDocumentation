Sits between the article body and the site footer, hairline rules above and below.

```jsx
<FeedbackBar value={vote} onVote={setVote} />
```

- Buttons are 1px grey outlines that fill teal once selected; 👍 / 👎 emoji are part of the label.
